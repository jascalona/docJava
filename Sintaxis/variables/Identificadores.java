package variables;
public class Identificadores {
    public static void main(String[] args) {

        /*
         * Todas las variables de Java deben identificarse con nombres únicos .
         * 
         * Estos nombres únicos se llaman identificadores .
         * 
         * Los identificadores pueden ser nombres cortos (como x e y) o nombres más
         * descriptivos (edad, suma, volumen total).
         * 
         * Nota: Se recomienda utilizar nombres descriptivos para crear un código
         * comprensible y fácil de mantener:
         */

        // Resumen abreviado a una sola letra
        int m = 60;

        // Identificador descriptivo buenas practcas
        int minutesPerHour = 60;

        /**
         * Las reglas generales para nombrar variables son:
         * 
         * Los nombres pueden contener letras, dígitos, guiones bajos y signos de dólar.
         * 
         * Los nombres deben comenzar con una letra
         * 
         * Los nombres deben comenzar con una letra minúscula y no pueden contener espacios en blanco.
         * 
         * Los nombres también pueden comenzar con $ y _
         * 
         * Los nombres distinguen entre mayúsculas y minúsculas ("myVar" y "myvar" son variables diferentes)
         * 
         * Las palabras reservadas (como las palabras clave de Java, como into boolean) no se pueden usar como nombres
         */

    }
}
